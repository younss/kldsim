{
  "name": "kldsim",
  "private": true,
  "version": "0.1.0",
  "description": "KLD Sim — competitive enterprise architecture & business strategy simulation platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/llm-gateway && npm run build -w apps/api && npm run build -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w packages/llm-gateway && npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "test": "npm run test -w packages/shared && npm run test -w packages/llm-gateway && npm run test -w apps/api",
    "lint": "eslint . --ext .ts,.tsx",
    "prisma:generate": "npm run prisma:generate -w apps/api",
    "prisma:migrate": "npm run prisma:migrate -w apps/api",
    "prisma:seed": "npm run prisma:seed -w apps/api"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.16.11",
    "eslint": "^8.57.1",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0"
  }
}
